machine:
  node:
    version: 0.12.0
  java:
    version: openjdk8

branches:
  only:
    - master

dependencies:
  override:
    - "echo 'No extra dependency resolution'"

general:
  branches:
    ignore:
      - 'gh-pages'

deployment:
  website:
    branch: master
    owner: kameshsampath
    commands:
      - git config --global user.email "kamesh.sampath@hotmail.com"
      - git config --global user.name "kameshsampath"
      - ./tools/ci-docs.sh

