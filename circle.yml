machine:
  node:
    version: 0.12.0
  java:
    version: openjdk8

branches:
  only:
    - master

dependencies:
  override:
    - "echo 'No extra dependency resolution'"

general:
  branches:
    ignore:
      - 'gh-pages'
